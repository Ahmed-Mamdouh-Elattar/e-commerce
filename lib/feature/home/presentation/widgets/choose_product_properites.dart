import 'package:flutter/material.dart';

class ChooseProductProperites extends StatelessWidget {
  const ChooseProductProperites({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [

      ],
    );
  }
}
